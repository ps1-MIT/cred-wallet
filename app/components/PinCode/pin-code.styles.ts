import { StyleSheet, ViewStyle } from 'react-native';

interface PinCodeStyles {
  container: ViewStyle;
}

export const styles = StyleSheet.create<PinCodeStyles>({
  container: {
    flex: 1,
  },
});
